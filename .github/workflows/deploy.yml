# ─────────────────────────────────────────────────────────
# BelizeChain Node — CI/CD Pipeline
# Trigger: push to main  ·  manual dispatch
# Flow:    test → build → push ACR → deploy Azure VM
# ─────────────────────────────────────────────────────────

name: Build & Deploy Blockchain Node

on:
  push:
    branches: [belizechain]
  pull_request:
    branches: [belizechain]
  workflow_dispatch:

env:
  REGISTRY: belizechainacr.azurecr.io
  IMAGE_NAME: belizechain-node
  NODE_NAME: "BelizeChain-Testnet-Validator-1"

# Only one deploy at a time; cancel superseded runs
concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── 1. Run Rust unit tests ────────────────────────────
  test:
    name: 🧪 Rust Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.90.0"
          targets: wasm32-unknown-unknown

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler libclang-dev

      - name: Cache cargo registry & build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-test-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-test-

      - name: Run tests
        run: cargo test --release --workspace

  # ── 1c. Coverage gate (must reach 100%) ──────────────────────────────
  coverage:
    name: 📊 Coverage Gate (100%)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.90.0"
          targets: wasm32-unknown-unknown

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler libclang-dev

      - name: Cache cargo registry & build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-cov-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-cov-

      - name: Install cargo-tarpaulin
        run: cargo install cargo-tarpaulin --locked

      - name: Run coverage (enforce 100%)
        # .tarpaulin.toml is picked up automatically.
        # Excludes service.rs and rpc.rs (infrastructure layer).
        # Fails the job if coverage drops below 100%.
        run: cargo tarpaulin

      - name: Upload coverage report (Lcov → Codecov)
        uses: codecov/codecov-action@v4
        with:
          files: target/coverage/lcov.info
          fail_ci_if_error: false   # coverage upload is best-effort; gate is tarpaulin

  # ── 1b. Run pallet benchmarks (smoke test) ───────────
  benchmark:
    name: ⚖️ Benchmark Smoke Test
    runs-on: ubuntu-latest
    # Benchmarks are expensive — only run on push to main, not on PRs
    if: github.event_name != 'pull_request'
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.90.0"
          targets: wasm32-unknown-unknown,wasm32v1-none

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler libclang-dev

      - name: Cache cargo registry & build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-bench-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-bench-

      - name: Build with runtime-benchmarks feature
        run: cargo build --release --features runtime-benchmarks

      - name: Run benchmark smoke test (pallet-balances baseline)
        run: |
          ./target/release/belizechain-node benchmark pallet \
            --chain dev \
            --pallet pallet_balances \
            --extrinsic '*' \
            --steps 2 \
            --repeat 1 \
            --output /dev/null \
            2>&1 | head -50
        # Smoke test only — 2 steps, 1 repeat is enough to verify benchmarks compile and run.
        # Uses pallet_balances instead of frame_system to avoid the polkadot-sdk stable2512
        # frame-storage-access-test-runtime wasm-builder bug triggered by frame-system-benchmarking.
        # Full benchmarking (--steps 50 --repeat 20) should be run before releases.

  # ── 2. Build Docker image & push to ACR ───────────────
  build:
    name: 🐳 Build & Push Image
    needs: test
    runs-on: ubuntu-latest
    # Only build on pushes to main (not on PRs)
    if: github.event_name != 'pull_request'
    # ACR credentials (ACR_USERNAME / ACR_PASSWORD) must be valid in GitHub Secrets.
    # If login fails with 403, rotate the service principal in Azure Portal and
    # update the secrets at: Settings → Secrets and variables → Actions → Secrets.
    # P0-08 FIX: Removed continue-on-error: true — failed builds must NOT trigger deployment.
    outputs:
      image_tag: ${{ steps.meta.outputs.tags }}

    steps:
      - uses: actions/checkout@v4

      - name: Generate image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          # P0-10 FIX: Use immutable SHA-based tag only. Removed mutable :latest tag
          # to prevent tag mutation attacks.
          tags: |
            type=sha,prefix=

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to ACR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache
          cache-to:   type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache,mode=max

  # ── 3. Deploy to AKS ──────────────────────────────────
  deploy:
    name: 🚀 Deploy to Testnet (AKS)
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: testnet               # add environment protection rules in GitHub Settings

    steps:
      - uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Set AKS context
        uses: azure/aks-set-context@v4
        with:
          resource-group: ${{ secrets.AZURE_RESOURCE_GROUP }}
          cluster-name: ${{ secrets.AKS_CLUSTER_NAME }}

      - name: Create namespace (if needed)
        run: kubectl create namespace belizechain --dry-run=client -o yaml | kubectl apply -f -

      - name: Deploy to AKS
        run: |
          # P2P-FIX-003: Create ConfigMap for reserved nodes
          kubectl create configmap reserved-nodes \
            --from-file=reserved-nodes.txt=./scripts/deploy/reserved-nodes.txt \
            --namespace=belizechain \
            --dry-run=client -o yaml | kubectl apply -f -
          
          cat <<EOFK | kubectl apply -f -
          apiVersion: apps/v1
          kind: Deployment
          metadata:
            name: belizechain-node
            namespace: belizechain
            labels:
              app: belizechain-node
          spec:
            replicas: 1
            selector:
              matchLabels:
                app: belizechain-node
            template:
              metadata:
                labels:
                  app: belizechain-node
              spec:
                containers:
                  - name: belizechain-node
                    # P0-10 FIX: Use immutable SHA-pinned image tag
                    image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
                    ports:
                      - containerPort: 30333
                        name: p2p
                      - containerPort: 9944
                        name: rpc
                      - containerPort: 9615
                        name: metrics
                    args:
                      - "--chain"
                      - "/etc/belizechain/testnet-chain-spec-raw.json"
                      - "--base-path"
                      - "/data"
                      - "--name"
                      - "${{ env.NODE_NAME }}"
                      # P0-09 FIX: Use file-based node key instead of CLI arg (prevents /proc exposure)
                      - "--node-key-file"
                      - "/etc/belizechain/secrets/node-key"
                      - "--validator"
                      # P0-07 FIX: Removed --alice (well-known dev key). Validator keys
                      # must be injected via `author.insertKey` RPC or keystore file.
                      # P1-24 FIX: Removed --force-authoring (blocks produced while unsynced)
                      - "--reserved-nodes-file"
                      - "/etc/belizechain/reserved-nodes.txt"
                      - "--reserved-only"
                      - "--no-mdns"
                      - "--in-peers"
                      - "5"
                      - "--out-peers"
                      - "50"
                      - "--rpc-port"
                      - "9944"
                      - "--rpc-cors"
                      - "all"
                      - "--unsafe-rpc-external"
                      - "--rpc-methods"
                      - "Safe"
                      - "--prometheus-port"
                      - "9615"
                      - "--prometheus-external"
                      - "--trie-cache-size"
                      - "268435456"
                      - "--state-pruning"
                      - "archive"
                    volumeMounts:
                      - name: blockchain-data
                        mountPath: /data
                      - name: reserved-nodes
                        mountPath: /etc/belizechain
                        readOnly: true
                      - name: node-key
                        mountPath: /etc/belizechain/secrets
                        readOnly: true
                    # P1-21 FIX: Add security context
                    securityContext:
                      runAsNonRoot: true
                      runAsUser: 1000
                      allowPrivilegeEscalation: false
                      readOnlyRootFilesystem: true
                      capabilities:
                        drop: ["ALL"]
                    resources:
                      requests:
                        cpu: "500m"
                        # P0-11 FIX: Corrected YAML corruption (memory value was misplaced)
                        memory: "1Gi"
                      limits:
                        cpu: "1500m"
                        memory: "3Gi"
                volumes:
                  - name: blockchain-data
                    # P1-22 FIX: Use PVC instead of emptyDir to persist chain data across restarts
                    persistentVolumeClaim:
                      claimName: belizechain-data
                  - name: reserved-nodes
                    configMap:
                      name: reserved-nodes
                  # P0-09 FIX: Mount node key from K8s Secret
                  - name: node-key
                    secret:
                      secretName: belizechain-node-key
                      items:
                        - key: node-key
                          path: node-key
          ---
          apiVersion: v1
          kind: Service
          metadata:
            name: belizechain-node
            namespace: belizechain
          spec:
            selector:
              app: belizechain-node
            ports:
              - name: p2p
                port: 30333
                targetPort: 30333
              - name: rpc
                port: 9944
                targetPort: 9944
              - name: metrics
                port: 9615
                targetPort: 9615
            # P1-04 FIX: ClusterIP prevents direct internet exposure of RPC/metrics.
            # Use an Ingress or port-forward for authorized access.
            type: ClusterIP
          EOFK

      - name: Wait for rollout
        run: kubectl rollout status deployment/belizechain-node -n belizechain --timeout=300s

      - name: Verify deployment
        run: |
          kubectl get pods -n belizechain -o wide
          kubectl get svc -n belizechain
